import type { backendInterface, TrackMetadataPublic, ArtistTrackSummary, StoreCommentPublic, ArtistDashboardStats, RoyaltyDashboard, PayoutPublic, BankingInfoPublic, ArtistProfilePublic, ArtistAdminView, RealTimeDashboardStats, DownloadReportEntry } from "../backend";
import { ArtistStatus, LikeResult, PayoutStatus, TrackState, TrackType, UserRole } from "../backend";
import { Principal } from "@icp-sdk/core/principal";

const ok = <T,>(value: T) => ({ __kind__: "ok" as const, ok: value });
const err = (e: string) => ({ __kind__: "err" as const, err: e });

const sampleCoverArt = {
  getBytes: async () => new Uint8Array(),
  getDirectURL: () => "https://picsum.photos/seed/track1/300/300",
  withUploadProgress: (_fn: (p: number) => void) => sampleCoverArt,
} as any;

const sampleAudio = {
  getBytes: async () => new Uint8Array(),
  getDirectURL: () => "",
  withUploadProgress: (_fn: (p: number) => void) => sampleAudio,
} as any;

const sampleTracks: TrackMetadataPublic[] = [
  {
    id: BigInt(1),
    title: "Crown Anthem",
    trackType: TrackType.single,
    saleCount: BigInt(42),
    likeCount: BigInt(18),
    createdAt: BigInt(Date.now()) * BigInt(1000000),
    explicit: false,
    artistId: "artist-1",
    description: "The debut single from Chosen One Productions Distribution.",
    audioFile: sampleAudio,
    state: TrackState.published,
    previewCount: BigInt(150),
    genre: "Hip-Hop",
    preOrder: false,
    artistName: "The Chosen One",
    coverArt: { ...sampleCoverArt, getDirectURL: () => "https://picsum.photos/seed/crown/300/300" },
    priceInCents: BigInt(199),
    releaseDate: "2026-01-15",
    previewStartSecs: 0,
    previewEndSecs: 30,
    songDetails: "",
  },
  {
    id: BigInt(2),
    title: "Gold Rush",
    trackType: TrackType.single,
    saleCount: BigInt(28),
    likeCount: BigInt(11),
    createdAt: BigInt(Date.now()) * BigInt(1000000),
    explicit: false,
    artistId: "artist-2",
    description: "Smooth R&B vibes with golden production.",
    audioFile: sampleAudio,
    state: TrackState.published,
    previewCount: BigInt(89),
    genre: "R&B",
    preOrder: false,
    artistName: "Lyrical Legacy",
    coverArt: { ...sampleCoverArt, getDirectURL: () => "https://picsum.photos/seed/gold/300/300" },
    priceInCents: BigInt(149),
    releaseDate: "2026-02-10",
    previewStartSecs: 0,
    previewEndSecs: 30,
    songDetails: "",
  },
  {
    id: BigInt(3),
    title: "Empire State of Sound",
    trackType: TrackType.album,
    saleCount: BigInt(67),
    likeCount: BigInt(34),
    createdAt: BigInt(Date.now()) * BigInt(1000000),
    explicit: true,
    artistId: "artist-3",
    description: "Full album — 12 tracks of pure heat.",
    audioFile: sampleAudio,
    state: TrackState.published,
    previewCount: BigInt(312),
    genre: "Trap",
    preOrder: false,
    artistName: "Sovereign Sound",
    coverArt: { ...sampleCoverArt, getDirectURL: () => "https://picsum.photos/seed/empire/300/300" },
    priceInCents: BigInt(999),
    releaseDate: "2026-03-01",
    previewStartSecs: 0,
    previewEndSecs: 30,
    songDetails: "",
  },
];

const sampleComments: StoreCommentPublic[] = [
  {
    id: BigInt(1),
    authorId: "fan-1",
    body: "Absolute fire! Crown Anthem is on repeat. 🔥",
    createdAt: BigInt(Date.now() - 86400000) * BigInt(1000000),
    authorName: "MusicFan99",
    rating: BigInt(5),
  },
  {
    id: BigInt(2),
    authorId: "fan-2",
    body: "Gold Rush is smooth as silk. Highly recommend.",
    createdAt: BigInt(Date.now() - 172800000) * BigInt(1000000),
    authorName: "BeatLover",
    rating: BigInt(5),
  },
];

const sampleDashboardStats: ArtistDashboardStats = {
  totalTracks: BigInt(3),
  totalEarningsCents: BigInt(42350),
  totalPreviews: BigInt(551),
  totalSales: BigInt(137),
};

const samplePayouts: PayoutPublic[] = [
  {
    id: BigInt(1),
    status: PayoutStatus.processed,
    artistId: "artist-1",
    amountCents: BigInt(15000),
    processedAt: BigInt(Date.now() - 604800000) * BigInt(1000000),
    requestedAt: BigInt(Date.now() - 691200000) * BigInt(1000000),
  },
];

const sampleRoyaltyDashboard: RoyaltyDashboard = {
  trackBreakdown: [
    {
      title: "Crown Anthem",
      earningsCents: BigInt(7140),
      previews: BigInt(150),
      grossRevenueCents: BigInt(8400),
      sales: BigInt(42),
      trackId: BigInt(1),
    },
  ],
  availableBalanceCents: BigInt(27350),
  periodStats: {
    previews: BigInt(551),
    grossRevenueCents: BigInt(42350),
    sales: BigInt(137),
    periodLabel: "Last 30 Days",
    payoutCents: BigInt(36000),
  },
  payouts: samplePayouts,
};

const sampleBankingInfo: BankingInfoPublic = {
  routingNumber: "021000021",
  artistId: "artist-1",
  accountHolderName: "The Chosen One",
  bankName: "Chase Bank",
  updatedAt: BigInt(Date.now() - 2592000000) * BigInt(1000000),
  accountNumber: "****4242",
};

const sampleArtistProfile: ArtistProfilePublic = {
  id: "artist-1",
  bio: "Independent artist on Chosen One Productions Distribution. 85% royalties, real Stripe payments.",
  name: "The Chosen One",
  status: ArtistStatus.active,
  createdAt: BigInt(Date.now() - 7776000000) * BigInt(1000000),
};

const sampleArtistTracks: ArtistTrackSummary[] = [
  {
    id: BigInt(1),
    title: "Crown Anthem",
    trackType: TrackType.single,
    saleCount: BigInt(42),
    state: TrackState.published,
    previewCount: BigInt(150),
    genre: "Hip-Hop",
    priceInCents: BigInt(199),
    releaseDate: "2026-01-15",
  },
];

export const mockBackend: backendInterface = {
  _immutableObjectStorageBlobsAreLive: async () => [],
  _immutableObjectStorageBlobsToDelete: async () => [],
  _immutableObjectStorageConfirmBlobDeletion: async () => undefined,
  _immutableObjectStorageCreateCertificate: async () => ({ method: "PUT", blob_hash: "" }),
  _immutableObjectStorageRefillCashier: async () => ({}),
  _immutableObjectStorageUpdateGatewayPrincipals: async () => undefined,
  _initializeAccessControl: async () => undefined,
  addStoreComment: async () => ok(BigInt(Date.now())),
  adminChangePIN: async () => ok(null),
  adminDeleteArtist: async () => ok(null),
  adminListArtists: async (): Promise<{ __kind__: "ok"; ok: ArtistAdminView[] } | { __kind__: "err"; err: string }> => ok([]),
  adminLogin: async () => ok("mock-admin-token"),
  adminLogout: async () => ok(null),
  adminResetArtistPIN: async () => ok(null),
  adminSuspendArtist: async () => ok(null),
  adminUnsuspendArtist: async () => ok(null),
  askSupportChat: async () => ok("Thank you for your question! Our support team is here to help. If you need further assistance, please reach out through the Contact Us page."),
  assignCallerUserRole: async () => undefined,
  changePIN: async () => ok(null),
  createCheckoutSession: async () => "https://checkout.stripe.com/mock-session",
  createTrackCheckoutSession: async () => ok("https://checkout.stripe.com/mock-track-session"),
  deleteArtistAccount: async () => ok(null),
  deleteTrack: async () => ok(null),
  filterTracksByGenre: async () => sampleTracks,
  fulfillTrackPurchase: async () => ok(null),
  getArtistDashboardStats: async () => ok(sampleDashboardStats),
  getArtistProfile: async (_artistId: string) => ok(sampleArtistProfile),
  getArtistStoreLikes: async () => BigInt(0),
  getArtistTracks: async () => ok(sampleArtistTracks),
  getBankingInfo: async () => ok(sampleBankingInfo),
  getCallerUserRole: async () => UserRole.user,
  getDownloadReport: async (): Promise<{ __kind__: "ok"; ok: DownloadReportEntry[] } | { __kind__: "err"; err: string }> => ok([]),
  getMailingListSubscribers: async () => [],
  getMyArtistProfile: async () => sampleArtistProfile,
  getMyPayouts: async () => ok(samplePayouts),
  getPurchasedTracks: async () => ok([]),
  getCustomerDownloadReport: async (): Promise<{ __kind__: "ok"; ok: DownloadReportEntry[] } | { __kind__: "err"; err: string }> => ok([]),
  getCustomerPurchases: async (): Promise<{ __kind__: "ok"; ok: typeof sampleTracks } | { __kind__: "err"; err: string }> => ok([]),
  getRealTimeDashboardStats: async (): Promise<{ __kind__: "ok"; ok: RealTimeDashboardStats } | { __kind__: "err"; err: string }> => ok({ trackPreviews: BigInt(150), totalSales: BigInt(42), grossRevenue: BigInt(8399), artistPayout: BigInt(7139), totalListens: BigInt(551) }),
  getRecentNotifications: async () => [],
  getRoyaltyDashboard: async () => ok(sampleRoyaltyDashboard),
  getStripeSessionStatus: async () => ({ __kind__: "failed" as const, failed: { error: "Mock" } }),
  getStripeSessionStatus_internal: async () => ({ __kind__: "failed" as const, failed: { error: "Mock" } }),
  getTrack: async () => sampleTracks[0],
  getTrackLikes: async () => BigInt(0),
  getTracksByArtist: async (_artistId: string) => sampleTracks,
  hasLikedStore: async () => false,
  hasLikedTrack: async () => false,
  incrementPreviewCount: async () => undefined,
  isAdminSession: async () => false,
  isCallerAdmin: async () => false,
  isStripeConfigured: async () => true,
  likeArtistStore: async () => LikeResult.ok,
  likeTrack: async () => LikeResult.ok,
  listPublishedTracks: async () => sampleTracks,
  listStoreComments: async () => sampleComments,
  loginArtist: async () => ok("mock-session-token"),
  loginCustomer: async () => ok("mock-customer-token"),
  logoutArtist: async () => ok(null),
  logoutCustomer: async () => undefined,
  notifyArtistJoined: async () => undefined,
  notifyNewComment: async () => undefined,
  notifyNewSale: async () => undefined,
  notifyTrackUploaded: async () => undefined,
  publishTrack: async () => ok(null),
  requestPayout: async () => ok(BigInt(1)),
  getArtistSecurityQuestion: async (_artistName: string): Promise<string | null> => "What is the name of your first pet?",
  resetPINWithSecurityAnswer: async () => ok(null),
  saveBankingInfo: async () => ok(null),
  searchTracks: async () => sampleTracks,
  setArtistSecurityQuestion: async () => ok(null),
  setStripeConfiguration: async () => undefined,
  signupArtist: async () => ok(sampleArtistProfile),
  signupCustomer: async () => ok("mock-customer-token"),
  subscribeToMailingList: async () => ok(null),
  transform: async () => ({ status: BigInt(200), body: new Uint8Array(), headers: [] }),
  unlikeArtistStore: async () => LikeResult.notLiked,
  unlikeTrack: async () => LikeResult.notLiked,
  unpublishTrack: async () => ok(null),
  updateArtistProfile: async () => ok(null),
  updateArtistSocialLinks: async () => ok(null),
  updateTrackMetadata: async () => ok(null),
  uploadTrack: async () => ok(BigInt(Date.now())),
  validateSession: async () => ok("artist-1"),
  validateCustomerSession: async () => true,
  adminInitializePIN: async () => ok(null),
};
